---
description: Locate code with the cheap scout agent instead of searching in main context
argument-hint: [what you are looking for]
---

Use the `scout` subagent to locate the following in this repository, then
report back only what it found. Do not search yourself first — delegate.

Target: $ARGUMENTS

After scout returns, state in one sentence what you plan to do next, and wait
for confirmation before reading further files or making changes.
